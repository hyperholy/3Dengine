/*
	Should only consider functionality in regards to setting UP a scene, (everything done once at the start)
*/
#ifndef SCENE_H
#define SCENE_H

#include "graphics_structs.h"
#include <vector>

void setup_scene(Scene_t& rendererScene);
#endif